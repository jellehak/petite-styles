{
  "name": "petite-ui",
  "version": "0.0.1",
  "description": "A lightweight UI",
  "main": "index.js",
  "scripts": {
    "build": "npx postcss style.css -o dist/style.css -u postcss-import --no-map"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "postcss": "^8.4.20",
    "postcss-cli": "^10.1.0",
    "postcss-import": "^15.1.0"
  }
}
